<div class="text-center">
  <h1>新規登録</h1>
  <p>※登録後、全ての機能を無料にてご利用いただけます。</p>
</div>

<div class="row">
  <div class="col-sm-6 offset-sm-3">

    <%= form_with(model: @user, local: true) do |f| %>
       <%= render 'layouts/error_messages', model: f.object %>
      
      <div class="form-group">
        <%= f.label :name, '名前 or ニックネーム' %><span class="form-color">※必須</span>
        <%= f.text_field :name, class: 'form-control' %>
      </div>

      <div class="form-group">
        <%= f.label :email, 'メールアドレス' %> <i class="far fa-envelope"></i><span class="form-color">※必須</span>
        <%= f.email_field :email, class: 'form-control' %>
      </div>

      <div class="form-group">
        <%= f.label :password, 'パスワード(半角英数字6~12文字、各１文字以上を含む)' %><span class="form-color">※必須</span>
        <%= f.password_field :password, class: 'form-control' %>
      </div>

      <div class="form-group">
        <%= f.label :password_confirmation, 'パスワード再入力' %><span class="form-color">※必須</span>
        <%= f.password_field :password_confirmation, class: 'form-control' %>
      </div>
      
      <div class="form-group">
        <%= f.label :image, 'アイコン画像' %> <i class="far fa-smile"></i>
        <%= f.file_field :image, class: 'form-control' %>
      </div>
      
      <div class="form-group text-center">
        <%= f.label :unnotification, '新着通知メールが不要の場合チェック' %> 
        <%= f.check_box :unnotification, checked: false %>
      </div>
      
      <p>本サービスへ登録した時点で、<%= link_to'利用規約', terms_path %>に同意したものとします</p>
      <%= f.submit '利用規約に同意して登録', class: 'btn btn-primary btn-block' %>
    <% end %>
  </div>
</div>